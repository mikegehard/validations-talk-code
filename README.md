# Examples for Validations talk

## Running

There are also main programs for each type of error handling mechanism. You can run these via `sbt run-main`.

For example:
```
$ sbt "run-main validations.exceptions.Main"
```
